server:
  port: 8601

spring:
  application:
    name: helloservice
  boot:
    admin:
      client:
        url: http://localhost:8080
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8800/eureka/